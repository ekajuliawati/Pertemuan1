fun main() {
    // variabel statis dan dinamis
    var dinamis = "Hero"
    var statis = 2.0
    println(statis)

     // variabel dinamis tidak memerlukan adanya deklarasi tipe data pada sebuah variabel
    var dynamicVar = 2
    println ("Contoh dynamic variabel = " + dynamicVar)
    // variabel statis di harus kan untuk mendeklarasikan jenis tipe data nya
    var staticVar: Char = 'A'
    println("Contoh static variabel" + staticVar)
}